# MEMORY.md (jomission) — verified reusable lessons only

Scope: this project. Global lessons live in the global MEMORY.md.
Never store current state, secrets, or unverified claims here.
Canonical truth stays in manifests/; this file is a compaction-injection view.
Format per lesson: {trigger, cause, repair, evidence, scope}.

## Lessons

- L1-DOMAIN-SKILL-NOT-LOADED: trigger: jomission/qualification/intrinsic.py (V2.1c design numerics) written without loading the biophysical-modeling skill / cause: no startup step routes modelling work to domain skills; skill loading depended on the agent noticing / repair: jomission-gate-runner fronts every execution; its step 1 now names domain skills to load (added from this lesson) / evidence: manifests/harness/checkpoint_1.json skills.defect_found / scope: project procedure
- L2-STATE-PINNED-IN-TEST: trigger: tier-0 failed after a legitimate TODO status change (SCI-V21C-BOUNDARY OPEN_AFTER_HARNESS -> OPEN) / cause: tests/test_harness_state.py::test_preserved_science_todo asserted checkpoint-time statuses and verdicts / repair: test now asserts durable invariants only (items exist in a live status; LOCKED gates stay LOCKED); statuses validated against evidence by validate_current_state / evidence: pytest output 2026-09-16: assert 'OPEN' == 'OPEN_AFTER_HARNESS' / scope: project tests: never encode current state in a test
- L3-STATUS-PIN-RECURRENCE: trigger: owner retired the V2 chain 2026-09-19 and tier-0 failed on test_preserved_science_todo asserting V2.2-RECURRENCE and BLIND-OMISSION are LOCKED / cause: the test still encoded a current status despite its own durable-invariants comment; the L2 repair did not cover the chain assertion / repair: the chain assertion now accepts the durable set {LOCKED, RETIRED}; a retired chain can never become executable without a new authorization / evidence: commit acfc138; tier-0 green after / scope: project tests: status assertions must name the full terminal set, not the current value
- L4-NPZFILE-CLOSE-BEFORE-TEMP-CLEANUP: trigger: WinError 32 on TemporaryDirectory cleanup in the t4_t5 and t7 artifact tests / cause: the tests' own np.load of an npz keeps the zip handle open on win32; no mmap exists in the analysis modules / repair: close the NpzFile before the temp context exits; test-harness only, no quarantine / evidence: commit 9aa6283; serial reruns 31.39s and 26.70s with no PermissionError / scope: windows test hygiene: every npz load in a temp dir
- L5-DYNAMICSTATE-REPLACE-NOT-CONSTRUCT: trigger: five Q8 tests failed with TypeError: DynamicState.__new__() missing theta_S, aux, b after the 0.4.24 migration / cause: state_replacement.py constructed the state from a hardcoded six-field list that predates the engine's NamedTuple / repair: rebuild through the namedtuple's own _replace (undeclared fields preserved by construction) and derive hash/shape/verify field lists from DynamicState._fields / evidence: commit 8a0450f; tests/test_q8_phenotype.py 10 passed, 2 skipped / scope: any code that builds engine state: replace, never reconstruct
- L6-NO-DETACHED-RUNS: trigger: tier-2 foreground run aborted twice by the owner; the detached relaunch was killed by the harness (ChildProcess.kill on the spawning shell) / cause: this environment has no background execution: long foreground runs get aborted and detached processes are reaped / repair: run long batteries only in the foreground with the owner present, or split them; do not relaunch detached / evidence: 2026-09-19 session: two owner aborts, one harness kill, zero output / scope: this environment: scheduling of minute-plus runs
- L7-PY-EDIT-REFORMATS-FILE: trigger: editing jomission/*.py produced whole-file normalization on top of the intended change, four times: t4_t5_analysis.py 293+/93-, builder.py 181+/49-, hdp_attractor.py 1045+/554-, q8_phenotype.py and run.py in 39e8c18 / cause: edit-time formatter normalization: single-to-double quote flips plus black-style rewrap and blank-line insertion, all semantically null by construction; fires once per file (first edit), later edits stay small / repair: treat the first .py diff per file as suspect: confirm with git diff -w that the non-whitespace hunks are only the intended ones, then rely on the covering tests as the behavior arbiter / evidence: commits 78ab111, b9a6443, f8e1809, 39e8c18 diff stats; quote-normalization tells in 39e8c18 / scope: this environment: reviewing .py diffs
